{
  "routes": [
    {
      "method": "GET",
      "path": "/text-pages",
      "handler": "text-page.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/text-pages/count",
      "handler": "text-page.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/text-pages/:id",
      "handler": "text-page.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/text-pages",
      "handler": "text-page.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/text-pages/:id",
      "handler": "text-page.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/text-pages/:id",
      "handler": "text-page.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
